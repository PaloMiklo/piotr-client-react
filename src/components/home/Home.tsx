import React, { ReactElement } from 'react';
import MainWrapper from '../main-wrapper/MainWrapper';

const Home = (): ReactElement<Element, string> => {
    return (
        <div>
            <h1>Home Page</h1>
        </div>
    )
}

export default Home;