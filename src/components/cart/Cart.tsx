import { ReactElement } from 'react';
import './Cart.scss';

const Cart = (): ReactElement => {
    return (
        <div className="container">
            <h1>Cart Page</h1>
        </div>
    );
}

export default Cart;