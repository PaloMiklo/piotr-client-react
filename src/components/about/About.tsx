import { ReactElement } from 'react';
import './About.scss';

const About = (): ReactElement => {
    return (
        <div className="container">
            <h1>About Page</h1>
        </div>
    );
}

export default About;